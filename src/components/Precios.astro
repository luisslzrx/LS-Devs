<section class=" px-6 py-24 md:px-12 lg:pl-20 lg:pr-0 overflow-hidden">

  <!-- Header centrado -->
  <div class="text-center mb-16 apple-reveal">
    <h2 class="text-5xl md:text-7xl font-bold text-white mb-6">Nuestros precios</h2>
    <p class="text-base md:text-lg text-white/40 max-w-xl mx-auto leading-relaxed mb-10">
      Elige el plan que mejor se adapte a tu negocio. Sin costos ocultos.
    </p>
    <div class="flex flex-col items-center gap-2">
      <a href="#contacto"
        class="px-10 py-4 rounded-full border border-white/25 text-white text-base font-semibold hover:bg-white/10 hover:scale-105 active:scale-95 transition-all duration-200">
        Cotiza tu proyecto
      </a>
      <p class="text-xs text-white/30">respondo en menos de 24 h</p>
    </div>
  </div>

  <!-- Cards carousel -->
  <div id="preciosCarousel" class="relative z-10 flex gap-4 overflow-x-auto snap-x snap-mandatory pb-4 scrollbar-hide -mr-6 md:-mr-12 lg:-mr-20 pr-6 md:pr-12 lg:pr-20 apple-reveal" style="transition-delay: 150ms;">

    <!-- Card 1: Landing Page -->
    <div class="precio-card snap-start flex-shrink-0 w-[75vw] md:w-[300px] rounded-3xl p-6 flex flex-col min-h-[720px] transition-all duration-300"
      style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.08); backdrop-filter: blur(20px);">
      <div>
        <p class="text-xs text-white/40 mb-4">Landing Page</p>
        <div class="border-t border-white/10 mb-6"></div>
        <p class="text-4xl font-bold text-white mb-1">Desde $1,500 <span class="text-lg font-normal text-white/40">MXN</span></p>
        <p class="text-xs text-white/30 mb-6">Pago único</p>
      </div>
      <a href="#contacto" class="w-full py-3.5 rounded-full bg-white text-[#0d0d0d] text-sm font-bold text-center hover:bg-white/90 hover:scale-105 active:scale-95 transition-all duration-200 mb-8 block">
        Comenzar
      </a>
      <div class="flex flex-col gap-0 flex-1">
        <div class="flex items-center gap-2 text-xs text-white/40 mb-3">
          <svg class="w-4 h-4 flex-shrink-0" viewBox="0 0 24 24" fill="none" stroke="#50C1FC" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          Características incluidas:
        </div>
        <div class="flex flex-col gap-2">
          {["Dominio y hosting gratis por 1 año", "Diseño personalizado", "Desarrollo en WordPress o código", "1 página de alto impacto", "Diseño 100% responsive", "Formulario de contacto", "Botón de WhatsApp", "Optimización básica para SEO", "Integración con Google Maps", "Optimización de velocidad", "Entrega en 5 días hábiles"].map(f => (
            <div class="flex items-start gap-3 text-sm text-white/80">
              <svg class="w-3.5 h-3.5 mt-0.5 flex-shrink-0 text-white/50" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
              <span>{f}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- Card 2: Sitio Informativo -->
    <div class="precio-card snap-start flex-shrink-0 w-[75vw] md:w-[300px] rounded-3xl p-6 flex flex-col min-h-[720px] transition-all duration-300"
      style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.08); backdrop-filter: blur(20px);">
      <div>
        <p class="text-xs text-white/40 mb-4">Sitio Web Informativo</p>
        <div class="border-t border-white/10 mb-6"></div>
        <p class="text-4xl font-bold text-white mb-1">Desde $3,500 <span class="text-lg font-normal text-white/40">MXN</span></p>
        <p class="text-xs text-white/30 mb-6">Pago único</p>
      </div>
      <a href="#contacto" class="w-full py-3.5 rounded-full bg-white text-[#0d0d0d] text-sm font-bold text-center hover:bg-white/90 hover:scale-105 active:scale-95 transition-all duration-200 mb-8 block">
        Comenzar
      </a>
      <div class="flex flex-col gap-0 flex-1">
        <div class="flex items-center gap-2 text-xs text-white/40 mb-3">
          <svg class="w-4 h-4 flex-shrink-0" viewBox="0 0 24 24" fill="none" stroke="#50C1FC" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          Características incluidas:
        </div>
        <p class="text-sm text-white/50 mb-3 pl-6">Todo lo de Landing Page</p>
        <div class="flex flex-col gap-2">
          {["Hasta 4 secciones internas", "Menú de navegación", "Formularios personalizados", "Galería de imágenes", "Integración con redes sociales", "Optimización SEO", "Panel de administración", "Diseño escalable", "Entrega en 10 días hábiles"].map(f => (
            <div class="flex items-start gap-3 text-sm text-white/80">
              <svg class="w-3.5 h-3.5 mt-0.5 flex-shrink-0 text-white/50" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
              <span>{f}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- Card 3: Tienda — destacada -->
    <div class="precio-card snap-start flex-shrink-0 w-[75vw] md:w-[300px] rounded-3xl p-6 flex flex-col min-h-[720px] relative transition-all duration-300"
      style="background: rgba(80,193,252,0.08); border: 1px solid rgba(80,193,252,0.3); backdrop-filter: blur(20px);">
      <span class="absolute top-5 right-5 px-3 py-1 rounded-full text-xs font-semibold text-[#0d0d0d] bg-[#50C1FC]">Popular</span>
      <div>
        <p class="text-xs text-[#50C1FC]/70 mb-4">Tienda en Línea</p>
        <div class="border-t border-[#50C1FC]/20 mb-6"></div>
        <p class="text-4xl font-bold text-white mb-1">Desde $6,500 <span class="text-lg font-normal text-white/40">MXN</span></p>
        <p class="text-xs text-white/30 mb-6">Pago único</p>
      </div>
      <a href="#contacto" class="w-full py-3.5 rounded-full bg-[#50C1FC] text-[#0d0d0d] text-sm font-bold text-center hover:bg-[#38b0f0] hover:scale-105 active:scale-95 transition-all duration-200 mb-8 block">
        Comenzar
      </a>
      <div class="flex flex-col gap-0 flex-1">
        <div class="flex items-center gap-2 text-xs text-white/40 mb-3">
          <svg class="w-4 h-4 flex-shrink-0" viewBox="0 0 24 24" fill="none" stroke="#50C1FC" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          Características incluidas:
        </div>
        <p class="text-sm text-white/50 mb-3 pl-6">Todo lo de Sitio Web Informativo</p>
        <div class="flex flex-col gap-2">
          {["Dominio y hosting gratis por 1 año", "Hasta 100 productos incluidos", "Catálogo de productos", "Carrito de compras", "Pasarela de pagos (Stripe, Mercado Pago, etc.)", "Gestión de pedidos", "Cupones y descuentos", "Panel para administrar productos", "Cálculo de envíos", "Optimización SEO", "Entrega en 20 a 30 días hábiles"].map(f => (
            <div class="flex items-start gap-3 text-sm text-white/80">
              <svg class="w-3.5 h-3.5 mt-0.5 flex-shrink-0 text-[#50C1FC]/70" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
              <span>{f}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- Card 4: Mantenimiento Web -->
    <div class="precio-card snap-start flex-shrink-0 w-[75vw] md:w-[300px] rounded-3xl p-6 flex flex-col min-h-[720px] transition-all duration-300"
      style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.08); backdrop-filter: blur(20px);">
      <div>
        <p class="text-xs text-white/40 mb-4">Mantenimiento Web</p>
        <div class="border-t border-white/10 mb-6"></div>
        <p class="text-4xl font-bold text-white mb-1">Desde $500 <span class="text-lg font-normal text-white/40">MXN / mes</span></p>
        <p class="text-xs text-white/30 mb-6">Pago mensual</p>
      </div>
      <a href="#contacto" class="w-full py-3.5 rounded-full bg-white text-[#0d0d0d] text-sm font-bold text-center hover:bg-white/90 hover:scale-105 active:scale-95 transition-all duration-200 mb-8 block">
        Solicitar mantenimiento
      </a>
      <div class="flex flex-col gap-0 flex-1">
        <div class="flex items-center gap-2 text-xs text-white/40 mb-3">
          <svg class="w-4 h-4 flex-shrink-0" viewBox="0 0 24 24" fill="none" stroke="#50C1FC" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          Características incluidas:
        </div>
        <div class="flex flex-col gap-2">
          {["Actualización del sitio web", "Respaldo de información", "Monitoreo y seguridad", "Corrección de errores", "Optimización de velocidad", "Actualización de plugins y sistema", "Cambios de contenido", "Soporte técnico prioritario", "Reportes periódicos", "Asistencia por WhatsApp"].map(f => (
            <div class="flex items-start gap-3 text-sm text-white/80">
              <svg class="w-3.5 h-3.5 mt-0.5 flex-shrink-0 text-white/50" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
              <span>{f}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- Card 5: Desarrollo Personalizado -->
    <div class="precio-card snap-start flex-shrink-0 w-[75vw] md:w-[300px] rounded-3xl p-6 flex flex-col min-h-[720px] transition-all duration-300"
      style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.08); backdrop-filter: blur(20px);">
      <div>
        <p class="text-xs text-white/40 mb-4">Desarrollo Personalizado</p>
        <div class="border-t border-white/10 mb-6"></div>
        <p class="text-4xl font-bold text-white mb-1">Cotización personalizada</p>
        <p class="text-xs text-white/30 mb-6">A tu medida</p>
      </div>
      <a href="#contacto" class="w-full py-3.5 rounded-full bg-white text-[#0d0d0d] text-sm font-bold text-center hover:bg-white/90 hover:scale-105 active:scale-95 transition-all duration-200 mb-8 block">
        Solicitar cotización
      </a>
      <div class="flex flex-col gap-0 flex-1">
        <div class="flex items-center gap-2 text-xs text-white/40 mb-3">
          <svg class="w-4 h-4 flex-shrink-0" viewBox="0 0 24 24" fill="none" stroke="#50C1FC" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          Características incluidas:
        </div>
        <div class="flex flex-col gap-2">
          {["Soluciones totalmente personalizadas", "Desarrollo de sistemas web", "Integraciones con APIs", "Automatización de procesos", "Paneles administrativos", "Plataformas empresariales", "Tiendas en línea avanzadas", "Escalabilidad para crecer contigo", "Tecnologías modernas y seguras", "Presupuesto adaptado a tu proyecto", "Tiempo de entrega según el alcance del desarrollo"].map(f => (
            <div class="flex items-start gap-3 text-sm text-white/80">
              <svg class="w-3.5 h-3.5 mt-0.5 flex-shrink-0 text-white/50" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
              <span>{f}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

  </div>

</section>

<script is:inline>
(function () {
  const cards = document.querySelectorAll('.precio-card');

  cards.forEach(function (card) {
    card.addEventListener('mouseenter', function () {
      cards.forEach(function (c) {
        if (c !== card) {
          c.style.opacity = '0.25';
          c.style.filter  = 'blur(2px)';
          c.style.transform = 'scale(0.98)';
        } else {
          c.style.opacity = '1';
          c.style.filter  = 'none';
          c.style.transform = 'scale(1)';
        }
      });
    });

    card.addEventListener('mouseleave', function () {
      cards.forEach(function (c) {
        c.style.opacity = '1';
        c.style.filter  = 'none';
        c.style.transform = 'scale(1)';
      });
    });
  });
})();
</script>