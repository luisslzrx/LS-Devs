<!-- STATS -->
<section>
  <div class="max-w-6xl mx-auto pb-16 lg:pb-[70px]">

    <div class="grid grid-cols-2 lg:grid-cols-4 rounded-[22px] overflow-hidden"
         style="background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.12); backdrop-filter: blur(30px); -webkit-backdrop-filter: blur(30px);">

      <!-- Stat 1 -->
      <div class="relative px-3 sm:px-4 py-4 lg:px-5 lg:py-[22px] text-center transition-colors duration-200 hover:bg-white/[0.07]"
           style="background: rgba(255,255,255,0.04);">
        <div class="text-xl sm:text-2xl lg:text-[2.1rem] font-black tracking-[-0.05em] leading-none mb-1 lg:mb-[5px] bg-gradient-to-br from-blue-50 to-blue-400 bg-clip-text text-transparent">
          +80
        </div>
        <div class="text-xs sm:text-xs lg:text-[0.78rem] font-medium leading-[1.4] text-blue-300/55">
          Proyectos entregados
        </div>
        <!-- Divider derecho -->
        <div class="hidden lg:block absolute right-0 top-[15%] h-[70%] w-px bg-white/[0.09]"></div>
        <!-- Divider inferior móvil -->
        <div class="lg:hidden absolute bottom-0 left-[10%] right-[10%] h-px bg-white/9"></div>
      </div>

      <!-- Stat 2 -->
      <div class="relative px-3 sm:px-4 py-4 lg:px-5 lg:py-[22px] text-center transition-colors duration-200 hover:bg-white/[0.07]"
           style="background: rgba(255,255,255,0.04);">
        <div class="text-xl sm:text-2xl lg:text-[2.1rem] font-black tracking-[-0.05em] leading-none mb-1 lg:mb-[5px] bg-gradient-to-br from-blue-50 to-blue-400 bg-clip-text text-transparent">
          96%
        </div>
        <div class="text-xs sm:text-xs lg:text-[0.78rem] font-medium leading-[1.4] text-blue-300/55">
          Clientes satisfechos
        </div>
        <!-- Divider derecho desktop -->
        <div class="hidden lg:block absolute right-0 top-[15%] h-[70%] w-px bg-white/[0.09]"></div>
        <!-- Divider inferior móvil -->
        <div class="lg:hidden absolute bottom-0 left-[10%] right-[10%] h-px bg-white/9"></div>
      </div>

      <!-- Stat 3 -->
      <div class="relative px-3 sm:px-4 py-4 lg:px-5 lg:py-[22px] text-center transition-colors duration-200 hover:bg-white/[0.07]"
           style="background: rgba(255,255,255,0.04);">
        <div class="text-xl sm:text-2xl lg:text-[2.1rem] font-black tracking-[-0.05em] leading-none mb-1 lg:mb-[5px] bg-gradient-to-br from-blue-50 to-blue-400 bg-clip-text text-transparent">
          7d
        </div>
        <div class="text-xs sm:text-xs lg:text-[0.78rem] font-medium leading-[1.4] text-blue-300/55">
          Tiempo promedio de entrega
        </div>
        <!-- Divider derecho desktop -->
        <div class="hidden lg:block absolute right-0 top-[15%] h-[70%] w-px bg-white/[0.09]"></div>
        <!-- Divider inferior móvil -->
        <div class="lg:hidden absolute bottom-0 left-[10%] right-[10%] h-px bg-white/9"></div>
      </div>

      <!-- Stat 4 — sin divider -->
      <div class="px-3 sm:px-4 py-4 lg:px-5 lg:py-[22px] text-center transition-colors duration-200 hover:bg-white/[0.07]"
           style="background: rgba(255,255,255,0.04);">
        <div class="text-xl sm:text-2xl lg:text-[2.1rem] font-black tracking-[-0.05em] leading-none mb-1 lg:mb-[5px] bg-gradient-to-br from-blue-50 to-blue-400 bg-clip-text text-transparent">
          3×
        </div>
        <div class="text-xs sm:text-xs lg:text-[0.78rem] font-medium leading-[1.4] text-blue-300/55">
          Más conversiones promedio
        </div>
      </div>

    </div>
  </div>
</section>